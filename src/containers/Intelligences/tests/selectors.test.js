// import { selectIntelligencesDomain } from '../selectors';

describe('selectIntelligencesDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
